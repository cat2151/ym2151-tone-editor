[package]
name = "ym2151-tone-params"
version = "0.1.0"
edition = "2021"
description = "YM2151 FM tone parameter data model, random tone generation, and register encoding — Single Source of Truth shared by the native TUI app and the WASM crate"

[dependencies]
